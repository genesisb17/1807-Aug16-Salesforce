// Get this from github

// TERNARY -> [expression] ? [if yes] : [if no]
static String ternary(int x) {
    String test = x > 5 ? "x is greater than 5" : "x is less than 5";
    //return (x > 10) ? (x < 200 ? "x is less than 200") : " no ";
    return test;
}